# STS Strategies - Production Environment Variables Template
# Copy this file to .env.production and fill in the values

# ============================================================================
# DEPLOYMENT ENVIRONMENT
# ============================================================================
NODE_ENV=production

# ============================================================================
# DATABASE (PostgreSQL)
# ============================================================================
# Production PostgreSQL connection string
# Recommended providers: Neon (serverless), Supabase, Railway
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# ============================================================================
# REDIS (BullMQ Job Queue)
# ============================================================================
# Production Redis connection string
# Recommended providers: Upstash (serverless), Railway, Redis Cloud
# Format: redis://default:PASSWORD@HOST:PORT or rediss:// for TLS
REDIS_URL="redis://default:PASSWORD@HOST:PORT"

# Optional: Upstash Redis REST API (if using Upstash)
# UPSTASH_REDIS_REST_URL="https://YOUR-ENDPOINT.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="YOUR_TOKEN_HERE"

# ============================================================================
# NEXTAUTH (Authentication)
# ============================================================================
# Production URL (must match your domain)
NEXTAUTH_URL="https://yourdomain.com"

# Secret for NextAuth (MUST be different from staging/dev)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="REPLACE_WITH_PRODUCTION_SECRET"

# ============================================================================
# ENCRYPTION
# ============================================================================
# Key for encrypting sensitive credentials in database
# Generate with: openssl rand -base64 32
CREDENTIAL_ENCRYPTION_KEY="REPLACE_WITH_PRODUCTION_KEY"

# ============================================================================
# STRIPE (Payment Processing)
# ============================================================================
# Switch to LIVE mode keys (not test mode)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_live_YOUR_LIVE_SECRET_KEY"
STRIPE_PUBLISHABLE_KEY="pk_live_YOUR_LIVE_PUBLISHABLE_KEY"

# Webhook signing secret for production webhook endpoint
# Get from: https://dashboard.stripe.com/webhooks
# Endpoint URL: https://yourdomain.com/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET="whsec_YOUR_PRODUCTION_WEBHOOK_SECRET"

# Price ID for lifetime access product (LIVE mode)
# Create in Stripe dashboard or via API
STRIPE_PRICE_ID="price_YOUR_LIVE_PRICE_ID"

# ============================================================================
# RESEND (Email Service)
# ============================================================================
# Production API key
# Get from: https://resend.com/api-keys
RESEND_API_KEY="re_YOUR_PRODUCTION_API_KEY"

# Email address for sending emails (must be verified domain)
RESEND_FROM_EMAIL="noreply@yourdomain.com"

# ============================================================================
# ADMIN CONFIGURATION
# ============================================================================
# Admin email address (receives notifications, has admin access)
ADMIN_EMAIL="admin@yourdomain.com"

# ============================================================================
# TRADINGVIEW (Strategy Provisioning)
# ============================================================================
# TradingView account credentials for granting strategy access
# IMPORTANT: Use a dedicated account for automation
TRADINGVIEW_USERNAME="your_tradingview_username"
TRADINGVIEW_PASSWORD="your_tradingview_password"

# ============================================================================
# MONITORING & ERROR TRACKING (Optional but Recommended)
# ============================================================================
# Sentry DSN for error tracking
# Get from: https://sentry.io/settings/projects/
# SENTRY_DSN="https://YOUR_SENTRY_DSN@sentry.io/PROJECT_ID"

# Sentry environment
# SENTRY_ENVIRONMENT="production"

# ============================================================================
# ANALYTICS (Optional)
# ============================================================================
# Google Analytics 4 Measurement ID
# Get from: https://analytics.google.com/
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# ============================================================================
# FEATURE FLAGS (Optional)
# ============================================================================
# Enable/disable features in production
# ENABLE_REGISTRATION=true
# ENABLE_PAYMENTS=true
# MAINTENANCE_MODE=false

# ============================================================================
# RATE LIMITING (Optional)
# ============================================================================
# Rate limit configuration for API endpoints
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=900000

# ============================================================================
# LOGGING (Optional)
# ============================================================================
# Log level for production
# LOG_LEVEL="info"

# ============================================================================
# CORS (Optional)
# ============================================================================
# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# ============================================================================
# NOTES
# ============================================================================
# 1. NEVER commit this file with real values to version control
# 2. Use different secrets for production vs staging/dev
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Store secrets securely (use Vercel/Railway secret management)
# 5. Test all integrations in staging before deploying to production
# 6. Keep a backup of production secrets in a secure password manager
